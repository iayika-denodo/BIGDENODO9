# REQUIRES-PROPERTIES-FILE - # Do not remove this comment!
CREATE OR REPLACE DATASOURCE JDBC my_testie
    FOLDER = '/1 - connectivity/1 - data sources'
    DRIVERCLASSNAME = 'com.mysql.jdbc.Driver'
    DATABASEURI = '${databases.sql_db.folder.1 - connectivity.folder.1 - data sources.datasources.jdbc.my_testie.DATABASEURI}'
    USERNAME = '${databases.sql_db.folder.1 - connectivity.folder.1 - data sources.datasources.jdbc.my_testie.USERNAME}'
    USERPASSWORD = '${databases.sql_db.folder.1 - connectivity.folder.1 - data sources.datasources.jdbc.my_testie.USERPASSWORD}' ${databases.sql_db.folder.1 - connectivity.folder.1 - data sources.datasources.jdbc.my_testie.USERPASSWORD.ENCRYPTED}
    CLASSPATH = 'mysql9'
    DATABASENAME = 'Generic'
    DATABASEVERSION = ''
    FETCHSIZE = 1000
    VALIDATIONQUERY = ''
    INITIALSIZE = 4
    MAXIDLE = -1
    MINIDLE = 0
    MAXACTIVE = 20
    EXHAUSTEDACTION = 1
    TESTONBORROW = true
    TESTONRETURN = false
    TESTWHILEIDLE = false
    TIMEBETWEENEVICTION = -1
    NUMTESTPEREVICTION = 3
    MINEVICTABLETIME = 1800000
    POOLPREPAREDSTATEMENTS = false
    MAXOPENPREPAREDSTATEMENTS = -1
    DATA_LOAD_CONFIGURATION (
        BATCHINSERTSIZE = 200
    )
    SOURCECONFIGURATION (
        delegatescalarfunctionslist = (abs, ceil, coalesce, concat, convert_timezone(evaluate_literal), div, floor, getday, gethour, getminute, getmonth, getsecond, getyear, len, localtimestamp(evaluate_literal), log, lower, mod, mult, now, nullif, position, pow, power, replace, round, row_number, sqrt, subtract, sum, textcat, trim, upper)
    );

